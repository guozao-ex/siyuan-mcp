# 思源笔记 MCP 插件 - 重新设计

## 新的设计理念

### 插件的唯一作用
**启动和管理 MCP 服务器，让外部 AI Agent（Claude Desktop）可以访问思源笔记**

### 插件不做什么
- ❌ 不提供 AI 对话界面
- ❌ 不直接调用 AI API
- ❌ 不实现快捷操作
- ❌ 不需要复杂的 UI

### 插件只做什么
- ✅ 启动/停止 MCP 服务器
- ✅ 提供一个设置面板（管理菜单项）
- ✅ 显示 MCP 服务器状态
- ✅ 配置 MCP 端口

---

## 简化后的插件界面

### 1. 管理菜单中的设置项
```
⚙️ 设置
  ├── 📦 新枝 Newledge
  ├── 🤖 MCP 服务器              ← 我们的插件
  ├── ⚙️ 移除按钮
  └── ...
```

### 2. 设置面板（点击后打开）
```
╔══════════════════════════════════════╗
║  🤖 MCP 服务器设置                   ║
╠══════════════════════════════════════╣
║                                      ║
║  📊 服务器状态                        ║
║  ┌────────────────────────────────┐ ║
║  │ ● 运行中                       │ ║
║  │ 端口: 3000                     │ ║
║  │ 连接数: 1                      │ ║
║  └────────────────────────────────┘ ║
║                                      ║
║  ⚙️ 配置                             ║
║  ┌────────────────────────────────┐ ║
║  │ 启动模式:                      │ ║
║  │ ○ 自动启动                     │ ║
║  │ ● 手动启动                     │ ║
║  │                                │ ║
║  │ 监听端口: [3000]               │ ║
║  │                                │ ║
║  │ 传输模式:                      │ ║
║  │ ☑ HTTP                         │ ║
║  │ ☐ stdio                        │ ║
║  └────────────────────────────────┘ ║
║                                      ║
║  🔗 连接信息                         ║
║  ┌────────────────────────────────┐ ║
║  │ 复制配置到 Claude Desktop:     │ ║
║  │                                │ ║
║  │ {                              │ ║
║  │   "siyuan": {                  │ ║
║  │     "url": "http://..."        │ ║
║  │   }                            │ ║
║  │ }                              │ ║
║  │                                │ ║
║  │ [📋 复制配置]                  │ ║
║  └────────────────────────────────┘ ║
║                                      ║
║  [启动服务器] [停止服务器]          ║
╚══════════════════════════════════════╝
```

### 3. 顶部状态栏（可选）
```
思源笔记顶部：  [...] [🤖●] [...]
                      ↑
                   绿点表示运行中
                   点击显示状态信息
```

---

## 实现方案

### 方案 A：插件启动独立进程（推荐）
```
思源插件 → 启动 Node.js 进程 → MCP 服务器运行
```
**优点：** 真正的服务器，外部可访问
**缺点：** 需要处理进程管理

### 方案 B：用户手动启动（简单）
```
用户手动运行: npm run start-mcp
插件只显示状态和配置信息
```
**优点：** 插件超简单
**缺点：** 用户需要额外操作

---

## 使用流程

### 用户视角
1. 安装插件
2. 在设置中启动 MCP 服务器
3. 复制配置到 Claude Desktop
4. 在 Claude Desktop 中使用 "@思源笔记" 操作笔记
5. 插件只是在后台保持 MCP 服务器运行

### 典型对话
```
用户（在 Claude Desktop）:
"@思源笔记 搜索关于AI的笔记"

Claude:
[通过 MCP 调用 search_notes API]
找到了3篇相关笔记：
1. AI 基础知识
2. 机器学习入门
3. ...

用户:
"帮我在笔记本中创建一篇新笔记，总结这些内容"

Claude:
[通过 MCP 调用 create_document API]
已创建笔记《AI 知识总结》
```

---

## 您想选择哪种方案？

1. **方案 A** - 插件启动真实的 MCP 服务器（复杂但完整）
2. **方案 B** - 插件只提供设置界面，用户手动启动服务器（简单）
3. **其他想法？**
